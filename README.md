# Commands:

- `npx prisma studio` - to manipulate the db data
- `npx prisma generate` - to update types
- `npx prisma db push` or `npx prisma migrate dev --name <migration file name>` - to apply schema updates to db
